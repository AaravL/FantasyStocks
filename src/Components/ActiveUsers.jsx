import React, { useState, useEffect, useRef } from "react";

export default function ActiveUsers({ leagueId, userId }) { 

    const [userStatuses, setUserStatuses] = useState(null); // Should be an empty map?

    return (
        <div>

        </div>
    );
}